'use strict';

var assert = require('chai').assert;
var plotlyBasisTransform = require('../');

describe('plotly-basis-transform', function () {
  it('passes a test', function () {
    assert(plotlyBasisTransform());
  });
});
